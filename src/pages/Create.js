import React from 'react'

const Create = () => {
    return (
        <div>
            <h1>Create List</h1>
            Display Hea
            Create list format (term:definition)
            Input box with scroll bar
            <button id="create">Create</button>
        </div>
    )
}

export default Create
